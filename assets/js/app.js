document.addEventListener('DOMContentLoaded',()=>{
const b=document.querySelector('.menu-button'),m=document.querySelector('.mobile-menu'),back=document.querySelector('.menu-backdrop');const close=()=>{m?.classList.remove('open','is-open');back?.classList.remove('open');b?.setAttribute('aria-expanded','false');m?.setAttribute('aria-hidden','true');document.body.classList.remove('menu-open','nav-open')};const open=()=>{m?.classList.add('open');back?.classList.add('open');b?.setAttribute('aria-expanded','true');m?.setAttribute('aria-hidden','false');document.body.classList.add('menu-open','nav-open')};b?.addEventListener('click',()=>b.getAttribute('aria-expanded')==='true'?close():open());back?.addEventListener('click',close);m?.addEventListener('click',e=>e.target.closest('a')&&close());document.addEventListener('keydown',e=>e.key==='Escape'&&close());addEventListener('resize',()=>innerWidth>900&&close());
document.querySelectorAll('[data-filter]').forEach(btn=>btn.addEventListener('click',()=>{const v=btn.dataset.filter;document.querySelectorAll('[data-filter]').forEach(x=>x.classList.toggle('active',x===btn));document.querySelectorAll('.media-card').forEach(card=>{const show=v==='all'||card.dataset.category===v;card.hidden=!show;if(show&&card.animate)card.animate([{opacity:.2,transform:'translateY(8px)'},{opacity:1,transform:'none'}],{duration:420,easing:'cubic-bezier(.16,1,.3,1)'})})}));
document.querySelectorAll('.hover-trigger').forEach(el=>{el.addEventListener('pointermove',e=>{const r=el.getBoundingClientRect();el.style.setProperty('--mx',`${e.clientX-r.left}px`);el.style.setProperty('--my',`${e.clientY-r.top}px`)});el.addEventListener('pointerleave',()=>{el.style.setProperty('--mx','50%');el.style.setProperty('--my','50%')})});
const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;if(!reduced&&'IntersectionObserver'in window){const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('is-visible');io.unobserve(e.target)}}),{threshold:.1,rootMargin:'0px 0px -8%'});document.querySelectorAll('.reveal-premium').forEach(el=>io.observe(el))}else document.querySelectorAll('.reveal-premium').forEach(el=>el.classList.add('is-visible'));
const header=document.querySelector('.site-header');let last=scrollY,busy=false;addEventListener('scroll',()=>{if(busy)return;busy=true;requestAnimationFrame(()=>{const y=scrollY;header?.classList.toggle('header-scrolled',y>20);if(y>100)header?.classList.toggle('header-hidden',y>last+10);if(y<last-10)header?.classList.remove('header-hidden');last=y;busy=false})},{passive:true});
});