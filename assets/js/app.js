document.addEventListener('DOMContentLoaded',()=>{
 const button=document.querySelector('.menu-button'),menu=document.querySelector('.mobile-menu'),backdrop=document.querySelector('.menu-backdrop');
 const close=()=>{menu?.classList.remove('open','is-open');backdrop?.classList.remove('open');button?.setAttribute('aria-expanded','false');menu?.setAttribute('aria-hidden','true');document.body.classList.remove('menu-open','nav-open')};
 const open=()=>{menu?.classList.add('open');backdrop?.classList.add('open');button?.setAttribute('aria-expanded','true');menu?.setAttribute('aria-hidden','false');document.body.classList.add('menu-open','nav-open')};
 button?.addEventListener('click',()=>button.getAttribute('aria-expanded')==='true'?close():open());backdrop?.addEventListener('click',close);menu?.addEventListener('click',e=>{if(e.target.closest('a'))close()});document.addEventListener('keydown',e=>{if(e.key==='Escape')close()});window.addEventListener('resize',()=>{if(innerWidth>900)close()});
 document.querySelectorAll('[data-filter]').forEach(btn=>btn.addEventListener('click',()=>{const value=btn.dataset.filter;document.querySelectorAll('[data-filter]').forEach(x=>x.classList.toggle('active',x===btn));document.querySelectorAll('.media-card').forEach(card=>card.hidden=value!=='all'&&card.dataset.category!==value)}));
 document.querySelectorAll('.hover-trigger').forEach(el=>el.addEventListener('pointermove',e=>{const r=el.getBoundingClientRect();el.style.setProperty('--mx',`${e.clientX-r.left}px`);el.style.setProperty('--my',`${e.clientY-r.top}px`)}));
 const reveal=()=>document.querySelectorAll('.reveal-premium:not(.is-visible)').forEach(el=>{if(matchMedia('(prefers-reduced-motion: reduce)').matches||el.getBoundingClientRect().top<innerHeight*.9)el.classList.add('is-visible')});
 if('IntersectionObserver'in window&&!matchMedia('(prefers-reduced-motion: reduce)').matches){const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('is-visible');io.unobserve(e.target)}}),{threshold:.1,rootMargin:'0px 0px -8%'});document.querySelectorAll('.reveal-premium').forEach(el=>io.observe(el))}else{addEventListener('scroll',reveal,{passive:true});addEventListener('load',reveal);reveal()}
});
